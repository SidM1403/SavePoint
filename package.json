{
  "name": "questlog-root",
  "version": "1.0.0",
  "description": "Root package for running client and server concurrently",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "docker compose up -d && concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
